<div class="row">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        @(Html.OA().GridPage()
            .Name("gvWatchlist")
            .Header(header =>
            {
                header.Creator();
                header.Refresh();
            })
            .Read(Url.Action("Read", "WatchList", new { area = "Shows" }))
        )
    </div>
</div>